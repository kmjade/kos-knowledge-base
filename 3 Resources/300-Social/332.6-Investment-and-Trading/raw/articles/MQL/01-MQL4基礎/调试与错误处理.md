---
title: 除錯与错误處理
status: active
priority: high
tags: [resource/tech/programming/mql, mql4/debugging, mql4/error-handling]
aliases: [MQL4除錯技巧, MQL4错误處理]
created: 2026-02-01
updated: 2026-06-10
---

# 除錯与错误處理

# 方法

## 📚 除錯概述

# 方法
```mermaid
graph TB
# 方法
    A --> C[断点除錯]
# 分析
    A --> E[错误處理]
    
    B --> B1[Print函数]
# 顯示
    B --> B3[Alert弹窗]
    
    C --> C1[設置断点]
    C --> C2[单步执行]
    C --> C3[变量监视]
    
    D --> D1[日志檔案]
    D --> D2[错误代碼]
# 分析
    
    E --> E1[错误检测]
    E --> E2[异常處理]
    E --> E3[错误恢復]
```

## 🔍 Print除錯法

### Print函数使用

#### 基本Print用法
```mql4
// 基本Print輸出
void OnTick()
  {
   // 輸出简单資訊
   Print("OnTick执行");
   
   // 輸出变量值
   double price = Bid;
   Print("当前价格: ", price);
   
   // 輸出多個变量
   int ticket = 12345;
   double profit = 100.50;
   Print("訂單: ", ticket, " 利润: ", profit);
   
   // 格式化輸出
   string message = StringFormat("价格: %.5f, 手数: %.2f", Ask, 0.1);
   Print(message);
  }
```

#### 除錯資訊輸出
```mql4
// 函数入口和出口除錯
void MyFunction(double parameter)
  {
   Print("=== 进入函数 MyFunction ===");
   Print("参数: ", parameter);
   
   // 函数逻辑
   double result = parameter * 2;
   Print("計算結果: ", result);
   
   Print("=== 退出函数 MyFunction ===");
   return;
  }

// 条件除錯
void ConditionalDebug()
  {
   bool debugMode = true;  // 除錯开关
   
   if(debugMode)
     {
      Print("除錯資訊: 当前狀態");
     }
   
   // 主要逻辑
  }

// 效能除錯
void PerformanceTest()
  {
   uint startTime = GetTickCount();
   
   // 执行需要測試的代碼
   for(int i = 0; i < 1000; i++)
     {
      double ma = iMA(Symbol(), PERIOD_CURRENT, 20, 0, MODE_SMA, PRICE_CLOSE, 0);
     }
   
   uint endTime = GetTickCount();
   uint elapsedTime = endTime - startTime;
   
   Print("执行時間: ", elapsedTime, " 毫秒");
  }
```

# 顯示

# 顯示
```mql4
# 顯示
void DisplayDebugInfo()
  {
   string debugInfo = "";
   
   // 基本資訊
   debugInfo += "=== 除錯資訊 ===\n";
   debugInfo += "交易品种: " + Symbol() + "\n";
   debugInfo += "当前价格: " + DoubleToString(Bid, 5) + "\n";
   debugInfo += "当前時間: " + TimeToString(TimeCurrent()) + "\n";
   
   // 帳戶資訊
   debugInfo += "帳戶余额: " + DoubleToString(AccountBalance(), 2) + "\n";
   debugInfo += "持仓数量: " + IntegerToString(OrdersTotal()) + "\n";
   
   // 指标值
   double rsi = iRSI(Symbol(), PERIOD_CURRENT, 14, PRICE_CLOSE, 0);
   debugInfo += "RSI值: " + DoubleToString(rsi, 2) + "\n";
   
# 顯示
   Comment(debugInfo);
  }

void OnTick()
  {
   DisplayDebugInfo();
  }

void OnDeinit(const int reason)
  {
   Comment("");  // 清除圖表資訊
  }
```

## 🛑 断点除錯

### 設置和使用断点

#### 断点除錯步骤
```mql4
# 方法
// 1. 在代碼行号左侧点击設置断点
// 2. 按F5啟動除錯
// 3. 使用F10单步执行
// 4. 按F11进入函数
# 查看

int OnInit()
  {
   int x = 10;  // 在这里設置断点
   
   x = x + 5;   // 单步执行到这里
   Print("x的值: ", x);
   
   return(INIT_SUCCEEDED);
  }

void ComplexCalculation()
  {
   double result = 0;
   
   for(int i = 0; i < 100; i++)  // 在循环中設置断点
     {
      result += i;
      
      if(result > 1000)  // 条件断点
         break;
     }
   
   Print("最终結果: ", result);
  }
```

#### 变量监视
```mql4
# 查看
void MonitorVariables()
  {
   int counter = 0;
   double prices[10];
   
   for(int i = 0; i < 10; i++)
     {
      prices[i] = Close[i];
      counter++;  // 监视這個变量
     }
   
   // 在监视視窗中新增以下表达式：
# 查看
# 查看
# 查看
  }
```

# 分析

### 日志記錄

#### 詳細的日志記錄
```mql4
// 日志記錄函数
void LogMessage(string message, int level = 0)
  {
   // level: 0=資訊, 1=警告, 2=错误
   string levelStr = "";
   color levelColor = clrBlack;
   
   switch(level)
     {
      case 0:
         levelStr = "[INFO]";
         levelColor = clrBlue;
         break;
      case 1:
         levelStr = "[WARNING]";
         levelColor = clrOrange;
         break;
      case 2:
         levelStr = "[ERROR]";
         levelColor = clrRed;
         break;
     }
   
   string logMessage = TimeToString(TimeCurrent()) + " " + levelStr + " " + message;
   Print(logMessage);
  }

// 使用日志函数
void LogExample()
  {
   LogMessage("程式啟動", 0);
   
   double price = Ask;
   if(price > 0)
     {
      LogMessage("当前价格: " + DoubleToString(price, 5), 0);
     }
   else
     {
      LogMessage("价格获取失败", 2);
     }
   
   int orders = OrdersTotal();
   if(orders > 10)
     {
      LogMessage("持仓数量过多: " + IntegerToString(orders), 1);
     }
  }
```

### 错误日志

#### 交易错误記錄
```mql4
// 記錄交易错误
void LogTradeError(int error, string operation)
  {
   string errorMsg = ErrorDescription(error);
   string logMessage = StringFormat(
      "交易错误 - 操作: %s, 错误代碼: %d, 错误描述: %s",
      operation, error, errorMsg
   );
   
   Print(logMessage);
   
   // 可以将错误記錄到檔案
   WriteLogToFile(logMessage);
  }

// 寫入日志檔案
void WriteLogToFile(string message)
  {
   string filename = "TradingLog.txt";
   int handle = FileOpen(filename, FILE_WRITE|FILE_READ|FILE_TXT);
   
   if(handle != INVALID_HANDLE)
     {
      FileSeek(handle, 0, SEEK_END);
      FileWrite(handle, message);
      FileClose(handle);
     }
   else
     {
      Print("無法打開日志檔案");
     }
  }

// 使用示例
int ticket = OrderSend(Symbol(), OP_BUY, 0.1, Ask, 3, 0, 0, "Test", 0, 0, clrNONE);

if(ticket < 0)
  {
   int error = GetLastError();
   LogTradeError(error, "开仓");
  }
```

## ⚠️ 错误處理

### 常见错误类型

#### 運行时错误
```mql4
// 除零错误
void DivisionError()
  {
   int a = 10;
   int b = 0;
   
   // int result = a / b;  // 这会导致除零错误
   
   // 正确處理
   if(b != 0)
     {
      int result = a / b;
     }
   else
     {
      Print("错误: 除数不能为零");
     }
  }

// 数组越界错误
void ArrayOutOfBounds()
  {
   double prices[5];
   
   // prices[10] = 1.0;  // 数组越界错误
   
   // 正确處理
   if(10 < ArraySize(prices))
     {
      prices[10] = 1.0;
     }
   else
     {
      Print("错误: 数组索引超出范围");
     }
  }

// 空指针错误
void NullPointer()
  {
   // 确保在使用前檢查对象是否有效
   int handle = FileOpen("test.txt", FILE_READ);
   
   if(handle != INVALID_HANDLE)
     {
      // 安全使用檔案句柄
      string content = FileReadString(handle);
      FileClose(handle);
     }
   else
     {
      Print("错误: 檔案句柄無效");
     }
  }
```

### 交易错误處理

#### 交易函数错误處理
```mql4
// 安全的开仓函数
bool SafeOpenBuyOrder(double lots, double sl, double tp)
  {
   // 参数檢查
   if(lots <= 0)
     {
      Print("错误: 手数必须大于0");
      return false;
     }
   
   // 檢查市场狀態
   if(!MarketInfo(Symbol(), MODE_TRADEALLOWED))
     {
      Print("错误: 市场未開放");
      return false;
     }
   
   // 檢查帳戶资金
   double freeMargin = AccountFreeMargin();
   double requiredMargin = MarketInfo(Symbol(), MODE_MARGINREQUIRED) * lots;
   
   if(freeMargin < requiredMargin)
     {
      Print("错误: 保证金不足");
      return false;
     }
   
   // 执行交易
   ResetLastError();
   int ticket = OrderSend(
      Symbol(), OP_BUY, lots, Ask, 3, sl, tp, 
      "Safe Order", 0, 0, clrBlue
   );
   
   // 错误處理
   if(ticket < 0)
     {
      int error = GetLastError();
      Print("訂單失败: ", error, " - ", ErrorDescription(error));
      
      // 特定错误處理
      switch(error)
        {
         case 6:  // 没有連接
            Print("错误: 与伺服器没有連接");
            break;
         case 134:  // 保证金不足
            Print("错误: 帳戶资金不足");
            break;
         case 128:  // 交易超时
            Print("错误: 交易超时");
            break;
        }
      
      return false;
     }
   
   Print("訂單成功: ", ticket);
   return true;
  }

// 使用示例
void OnTick()
  {
   double sl = Ask - 50 * Point;
   double tp = Ask + 100 * Point;
   
   bool success = SafeOpenBuyOrder(0.1, sl, tp);
   
   if(success)
     {
      Print("訂單执行成功");
     }
  }
```

## 🔧 错误代碼和描述

### 错误代碼處理

#### 常用错误代碼
```mql4
// 错误代碼转换为描述字符串
string ErrorDescription(int error)
  {
   switch(error)
     {
      case 0:   return "没有错误";
      case 1:   return "没有错误，但結果未知";
      case 2:   return "通用错误";
      case 3:   return "错误的参数";
      case 4:   return "交易伺服器忙碌";
# 版本
      case 6:   return "没有連接";
      case 7:   return "权限不足";
      case 8:   return "请求过于频繁";
      case 9:   return "不允许的操作";
      case 64:  return "帳戶被禁止";
      case 65:  return "错误的账号";
      case 128: return "交易超时";
      case 129: return "错误的价格";
      case 130: return "错误的停止";
      case 131: return "错误的成交量";
      case 132: return "市场已關閉";
      case 133: return "交易被禁止";
      case 134: return "保证金不足";
      case 135: return "价格改变了";
      case 136: return "没有价格";
      case 137: return "经纪商忙";
      case 138: return "新报价";
      case 139: return "訂單被锁定";
      case 140: return "只允许买入";
      case 141: return "太多请求";
# 修改
      case 146: return "交易子系統忙碌";
      case 147: return "使用到期時間";
      case 148: return "訂單太多";
      case 149: return "对冲被禁止";
      case 150: return "禁止对冲";
      case 4000: return "没有错误";
      case 4001: return "错误的函数指针";
      case 4002: return "数组索引超出范围";
      case 4003: return "没有記憶體";
      case 4004: return "空指针";
      case 4005: return "格式化字符串错误";
      case 4006: return "数组作为参数不正确";
      case 4007: return "記憶體溢出";
      case 4008: return "棧溢出";
      case 4009: return "数组不是正确的大小";
      case 4010: return "除以零";
      case 4011: return "未知的命令";
      case 4012: return "错误的交易";
      case 4013: return "未知对象属性";
      case 4014: return "未知对象类型";
      case 4015: return "不支持的对象";
      case 4016: return "不支持的对象属性";
      case 4017: return "字符串連接错误";
      case 4018: return "内部错误";
      case 4019: return "字符串参数错误";
      case 4020: return "空字符串";
      case 4021: return "字符串太长";
      case 4022: return "未知的错误";
      case 4050: return "不正确的函数参数";
      case 4051: return "错误的函数指针";
      case 4052: return "数组索引超出范围";
      case 4053: return "没有記憶體";
      case 4054: return "空指针";
      case 4055: return "格式化字符串错误";
      case 4056: return "数组作为参数不正确";
      case 4057: return "記憶體溢出";
      case 4058: return "棧溢出";
      case 4059: return "数组不是正确的大小";
      case 4060: return "除以零";
      case 4061: return "未知的命令";
      case 4062: return "错误的交易";
      case 4063: return "未知对象属性";
      case 4064: return "未知对象类型";
      case 4065: return "不支持的对象";
      case 4066: return "不支持的对象属性";
      case 4067: return "字符串連接错误";
      case 4068: return "内部错误";
      case 4069: return "字符串参数错误";
      case 4070: return "空字符串";
      case 4071: return "字符串太长";
      case 4072: return "未知的错误";
      default:  return "未知错误代碼: " + IntegerToString(error);
     }
  }

// 使用错误描述函数
int ticket = OrderSend(Symbol(), OP_BUY, 0.1, Ask, 3, 0, 0, "Test", 0, 0, clrNONE);

if(ticket < 0)
  {
   int error = GetLastError();
   Print("错误代碼: ", error);
   Print("错误描述: ", ErrorDescription(error));
  }
```

## 🛡️ 防御性編程

### 参数驗證

#### 輸入参数檢查
```mql4
// 驗證輸入参数
bool ValidateParameters(double lotSize, int stopLoss, int takeProfit)
  {
   // 驗證手数
   double minLot = MarketInfo(Symbol(), MODE_MINLOT);
   double maxLot = MarketInfo(Symbol(), MODE_MAXLOT);
   
   if(lotSize < minLot)
     {
      Print("错误: 手数小于最小值 ", minLot);
      return false;
     }
   
   if(lotSize > maxLot)
     {
      Print("错误: 手数大于最大值 ", maxLot);
      return false;
     }
   
   // 驗證止损
   int stopLevel = (int)MarketInfo(Symbol(), MODE_STOPLEVEL);
   if(stopLoss < stopLevel)
     {
      Print("错误: 止损小于最小值 ", stopLevel);
      return false;
     }
   
   // 驗證止盈
   if(takeProfit < stopLevel)
     {
      Print("错误: 止盈小于最小值 ", stopLevel);
      return false;
     }
   
   return true;
  }

// 使用驗證函数
void SafeTrading()
  {
   double lotSize = 0.1;
   int stopLoss = 50;
   int takeProfit = 100;
   
   if(ValidateParameters(lotSize, stopLoss, takeProfit))
     {
      // 参数有效，可以执行交易
      int ticket = OrderSend(Symbol(), OP_BUY, lotSize, Ask, 3, 
                           Ask - stopLoss*Point, Ask + takeProfit*Point, 
                           "Validated Order", 0, 0, clrBlue);
      
      if(ticket > 0)
         Print("訂單成功: ", ticket);
     }
  }
```

### 狀態檢查

#### 交易環境檢查
```mql4
// 檢查交易環境
bool CheckTradingEnvironment()
  {
   // 檢查連接狀態
   if(!TerminalInfoInteger(TERMINAL_CONNECTED))
     {
      Print("错误: 终端未連接到伺服器");
      return false;
     }
   
   // 檢查交易是否允许
   if(!TerminalInfoInteger(TERMINAL_TRADE_ALLOWED))
     {
      Print("错误: 终端交易被禁止");
      return false;
     }
   
   // 檢查EA交易是否允许
   if(!AccountInfoInteger(ACCOUNT_TRADE_EXPERT))
     {
      Print("错误: EA交易未被帳戶允许");
      return false;
     }
   
   // 檢查市场狀態
   if(!MarketInfo(Symbol(), MODE_TRADEALLOWED))
     {
      Print("错误: 市场交易被禁止");
      return false;
     }
   
   return true;
  }

// 檢查帳戶狀態
bool CheckAccountStatus()
  {
   // 檢查帳戶类型
   bool isDemo = IsDemo();
   
   if(isDemo)
     {
      Print("警告: 使用模拟帳戶");
     }
   else
     {
      Print("警告: 使用真实帳戶，请谨慎操作");
     }
   
   // 檢查帳戶资金
   double balance = AccountBalance();
   if(balance < 100)
     {
      Print("警告: 帳戶余额过低: ", balance);
      return false;
     }
   
   // 檢查可用保证金
   double freeMargin = AccountFreeMargin();
   if(freeMargin < 100)
     {
      Print("警告: 可用保证金过低: ", freeMargin);
      return false;
     }
   
   return true;
  }

// 综合檢查
bool PreTradeCheck()
  {
   if(!CheckTradingEnvironment())
     {
      Print("交易環境檢查失败");
      return false;
     }
   
   if(!CheckAccountStatus())
     {
      Print("帳戶狀態檢查失败");
      return false;
     }
   
   return true;
  }

// 使用檢查函数
void OnTick()
  {
   if(PreTradeCheck())
     {
      // 檢查通過，可以执行交易
      Print("所有檢查通過，可以交易");
     }
  }
```

## 🎯 实战练习

### 练习1：除錯复杂函数

#### 任務要求
```mql4
// 除錯一个复杂的計算函数
// 功能：
// 1. 新增詳細的Print语句
// 2. 使用断点除錯
// 3. 記錄错误資訊
```

#### 參考代碼
```mql4
//+------------------------------------------------------------------+
//|                                    DebugPractice.mq4        |
//+------------------------------------------------------------------+
#property copyright "2026, Practice"
#property version   "1.00"
#property strict

// 复杂的計算函数
double ComplexCalculation(int period)
  {
   Print("=== 进入 ComplexCalculation ===");
   Print("参数: ", period);
   
   if(period <= 0)
     {
      Print("错误: 周期必须大于0");
      return 0;
     }
   
   if(period > Bars)
     {
      Print("错误: 周期大于K线数量");
      return 0;
     }
   
   double sum = 0.0;
   double count = 0;
   
   for(int i = 0; i < period; i++)
     {
      if(i < Bars)
        {
         double price = Close[i];
         sum += price;
         count++;
         
         if(i % 10 == 0)  // 每隔10次輸出一次
           {
            Print("處理进度: ", i, "/", period, " 当前价格: ", price);
           }
        }
     }
   
   double average = 0;
   if(count > 0)
     {
      average = sum / count;
      Print("总和: ", sum, " 数量: ", count, " 平均值: ", average);
     }
   else
     {
      Print("警告: 没有有效的數據");
     }
   
   Print("=== 退出 ComplexCalculation ===");
   return average;
  }

int OnInit()
  {
   // 設置断点在这里開始除錯
   Print("開始除錯复杂計算函数");
   
   double result = ComplexCalculation(20);
   Print("最终結果: ", result);
   
   return(INIT_SUCCEEDED);
  }
```

## 💡 最佳實踐

### 除錯技巧

#### 逐步除錯
- ✅ **設置断点**：在關鍵位置設置断点
- ✅ **单步执行**：逐步跟踪代碼执行
# 查看
- ❌ **盲目猜测**：避免没有根据的猜测

### 错误處理原則

#### 错误预防
- ✅ **参数驗證**：严格驗證所有輸入参数
- ✅ **環境檢查**：檢查交易環境和帳戶狀態
- ✅ **错误記錄**：詳細記錄所有错误資訊
- ❌ **忽略警告**：重视所有警告資訊

## 🔗 相關資源

- [[MQL4函数与控制流]] - 函数和控制流
- [[MQL4交易操作基礎]] - 交易操作
- [[Expert Advisor基礎]] - EA開發
- [[代碼規範与優化]] - 代碼品質

### 官方文檔

- **错误代碼**：https://www.mql5.com/en/docs/mql4/constants/errorswarnings/enum_error_codes
- **除錯工具**：https://www.mql5.com/en/articles/133

---
*創建時間: 2026-02-01*  
*分類: 3 Resources*
